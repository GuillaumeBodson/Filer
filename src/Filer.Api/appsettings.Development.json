{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Cors": {
    "_comment": "Dev origins of the Blazor WASM client (launchSettings.json). Real environments supply Cors__AllowedOrigins__0 etc. via env when the SPA is served cross-origin; leave empty behind a same-origin reverse proxy (05-security.md).",
    "AllowedOrigins": [
      "http://localhost:5067",
      "https://localhost:7291"
    ]
  },
  "Jwt": {
    "_comment": "Dev-only signing key. In real environments supply Jwt__SigningKey via env/secret store, never source control (05-security.md). Must be at least 32 characters.",
    "SigningKey": "dev-only-signing-key-change-me-please-32-bytes-min"
  },
  "Storage": {
    "_comment": "Dev-only blob root, relative to the API working directory. Real environments supply Storage__RootPath via env / volume mount (07-storage-and-deployment.md).",
    "RootPath": ".storage"
  }
}
